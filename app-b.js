function checkout(){let fee=Math.round(s.event.price*.05);return top()+`<main class="page"><div class="wrap"><div class="head"><div><span class="eyebrow">Test checkout</span><h1>Complete your ticket</h1><p class="muted">No external social account is requested.</p></div></div><div class="checkoutGrid"><form class="form" id="checkout"><div class="two"><div class="field"><label>Full name</label><input class="input" name="name" value="${esc(s.user.name)}" required></div><div class="field"><label>Email</label><input class="input" type="email" name="email" value="${esc(s.user.email)}" required></div><div class="field"><label>What describes you?</label><input class="input" name="interest" value="${esc(s.user.interest)}" required></div><div class="field"><label>Social energy</label><select class="input" name="energy"><option>Quiet</option><option selected>Balanced</option><option>Outgoing</option></select></div></div><div class="field"><label>What would make this event worthwhile?</label><textarea class="input" rows="4" name="goal">One useful conversation and a fresh perspective.</textarea></div><div class="field"><label>Demo card</label><input class="input" value="4242 4242 4242 4242" readonly></div><button class="btn coral block">Pay ${money(s.event.price+fee)}</button></form><aside class="summary"><span class="eyebrow">Order summary</span><h3>${esc(s.event.title)}</h3><div class="line"><span>Ticket</span><strong>${money(s.event.price)}</strong></div><div class="line"><span>Service fee</span><strong>${money(fee)}</strong></div><div class="line total"><span>Total</span><strong>${money(s.event.price+fee)}</strong></div><p class="note" style="color:#bbb5ab">Clicking Pay creates a real demo registration state but moves no money.</p></aside></div></div></main>`}
function ticket(){return top()+`<main class="page"><div class="wrap" style="max-width:720px"><section class="ticket"><div class="ticketTop"><span class="pill pGreen">Payment confirmed</span><h1>You are going.</h1><p>Your mix appears after the host releases groups.</p></div><div class="ticketMeta">${[['Attendee',s.user.name],['Ticket','General admission'],['Event',s.event.title],['Code','SMX-'+String(Date.now()).slice(-6)],['Date',date(s.event.date,false)],['Mix release','Host controlled']].map(x=>`<div class="meta"><small>${x[0]}</small><strong>${esc(x[1])}</strong></div>`).join('')}</div><div class="row" style="margin-top:19px"><button class="btn dark" data-go="/attendee">Open my event</button><button class="btn light" data-act="saveTicket">Save ticket</button></div></section></div></main>`}
function side(active){return `<aside class="side">${[['/host','Overview'],['/host/attendees','Attendees'],['/host/mixes','Mixes'],['/host/missions','Missions'],['/host/payouts','Payouts']].map(x=>`<a class="${active===x[0]?'on':''}" href="#${x[0]}">${x[1]}</a>`).join('')}</aside>`}function hostShell(a,c){return top()+`<div class="hostShell">${side(a)}<main class="hostMain">${c}</main></div>`}
function host(){let p=all().length,g=p*s.event.price,c=s.mixes.filter(m=>status(m)==='completed').length;return hostShell('/host',`<div class="head"><div><span class="eyebrow">Chief host control room</span><h1>${esc(s.event.title)}</h1><p class="muted">Sales, attendance, mixes, missions and payout.</p></div><button class="btn coral" data-go="/host/mixes">Manage mixes</button></div><div class="stats">${[['Paid attendees',p],['Gross sales',money(g)],['Mixes formed',s.mixes.length],['Missions verified',c]].map(x=>`<div class="stat"><small>${x[0]}</small><b>${x[1]}</b></div>`).join('')}</div><div class="dashGrid" style="margin-top:12px"><section class="panel"><span class="eyebrow">Event lifecycle</span><div class="timeline">${[['Event published',true,'Ticket page is live.'],['Registration',true,p+' paid attendees.'],['Mix release',s.event.mixesOut,s.event.mixesOut?s.mixes.length+' groups released':'Waiting for host'],['Event completion',s.event.ended,s.event.ended?'Evidence window open':'Mark complete after the show'],['Mission deadline',c>0,date(s.event.mission.deadline)]].map((x,i)=>`<div class="tl ${x[1]?'done':i===2&&!s.event.mixesOut?'current':''}"><span class="dot"></span><div><strong>${x[0]}</strong><span>${x[2]}</span></div></div>`).join('')}</div></section><aside class="panel"><span class="eyebrow">Mission design</span><h3>${esc(s.event.mission.name)}</h3><p class="muted">${esc(s.event.mission.brief)}</p><div class="deadline"><span>Evidence deadline</span><b>${date(s.event.mission.deadline)}</b></div></aside></div>`)}
function attendees(){return hostShell('/host/attendees',`<div class="head"><div><span class="eyebrow">Registration</span><h1>${all().length} attendees</h1><p class="muted">Only paid attendees enter the randomizer.</p></div></div><div style="overflow:auto"><table class="table"><thead><tr><th>Name</th><th>Interest</th><th>Energy</th><th>Paid</th><th>Checked in</th><th>Mix</th></tr></thead><tbody>${all().map(a=>`<tr><td><b>${esc(a.name)}</b><br><span class="muted">${esc(a.email||'')}</span></td><td>${esc(a.interest)}</td><td>${esc(a.energy)}</td><td style="color:var(--green)">Paid</td><td>${a.checked?'Yes':'—'}</td><td>${esc(s.mixes.find(m=>m.ids.includes(a.id))?.name||'—')}</td></tr>`).join('')}</tbody></table></div>`)}
function randomize(){let a=[...all()];for(let i=a.length-1;i;i--){let j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}s.mixes=[];let i=0;while(a.length>=3){s.mixes.push({id:'m'+Date.now()+i,name:N[i%N.length],ids:a.splice(0,3).map(x=>x.id),point:'Socialmix desk · Main entrance'});i++}s.standby=a.map(x=>x.id);s.event.mixesOut=false;s.evidence=[];save();toast(s.mixes.length+' mixes created');render()}
function mixes(){return hostShell('/host/mixes',`<div class="head"><div><span class="eyebrow">Group formation</span><h1>Randomize groups of three.</h1><p class="muted">No AI matching. No profile scraping. Any remainder stays on standby.</p></div>${s.event.mixesOut?'<span class="pill pGreen">Released</span>':''}</div><div class="mixGrid"><section class="mixList">${s.mixes.length?s.mixes.map((m,i)=>mixCard(m,i)).join(''):`<div class="empty"><h2>No mixes yet</h2><p class="muted">Run the randomizer after registration closes.</p></div>`}${s.standby.length?`<div class="panel"><b>${s.standby.length} attendee(s) on standby</b><p class="muted">No mix exceeds three people.</p></div>`:''}</section><aside class="control"><span class="eyebrow">Mix control</span><h2>${s.mixes.length?'Groups ready':'Close and mix'}</h2><p>Running again replaces the current groups. Release only when the host is satisfied.</p><div class="line"><span>Eligible</span><strong>${all().length}</strong></div><div class="line"><span>Complete groups</span><strong>${Math.floor(all().length/3)}</strong></div><button class="btn coral block" data-act="randomize">${s.mixes.length?'Run again':'Create mixes'}</button>${s.mixes.length&&!s.event.mixesOut?'<button class="btn light block" style="margin-top:8px" data-act="release">Release mixes</button>':''}${s.event.mixesOut?'<button class="btn light block" style="margin-top:8px" data-act="pause">Pause reveal</button>':''}</aside></div>`)}function mixCard(m,i){let ps=m.ids.map(id=>all().find(a=>a.id===id));return `<article class="mixCard"><div class="between"><div><span class="eyebrow">Mix ${String(i+1).padStart(2,'0')}</span><h3 style="margin:5px 0">${esc(m.name)}</h3></div>${pill(status(m))}</div><div class="memberGrid">${ps.map(p=>`<div class="member"><strong>${esc(p.name)}</strong><span>${esc(p.interest)} · ${esc(p.energy)}</span></div>`).join('')}</div><div class="missionMini"><strong>${esc(s.event.mission.name)}</strong><span>Evidence deadline ${date(s.event.mission.deadline)}</span></div></article>`}
function missionAction(m){let x=status(m);if(!s.event.ended)return '<span class="pill pGray">Opens after event</span>';if(x==='submitted')return '<span class="pill pViolet">Awaiting host review</span>';if(x==='completed')return '<span class="pill pGreen">Host verified</span>';return `<button class="btn dark small" data-act="evidence" data-mix="${m.id}">${x==='returned'?'Update evidence':'Submit evidence'}</button>`}
function attendee(){if(!s.user.paid)return top()+`<main class="page"><div class="wrap empty"><h2>No ticket yet</h2><p class="muted">Register before joining a mix.</p><button class="btn coral" data-go="/event">View event</button></div></main>`;let m=meMix();if(!s.event.mixesOut||!m)return top()+`<main class="page"><div class="wrap portalGrid"><section class="heroPortal"><span class="pill pGreen">Ticket confirmed</span><h1>Your place is saved.</h1><p>The host has not released mixes yet. Paid attendees will be randomized into groups of three.</p><button class="btn coral" data-go="/ticket">View ticket</button></section><aside class="panel"><b>Mix release</b><p class="muted">Host controlled. You will see two people, a meeting point and the mission.</p></aside></div></main>`;let ps=m.ids.map(id=>all().find(a=>a.id===id)),e=ev(m);return top()+`<main class="page"><div class="wrap portalGrid"><div><section class="heroPortal"><span class="pill pViolet">Mix released</span><h1>Meet ${esc(m.name)}.</h1><p>Three attendees, one event and one mission. Evidence is due after the event.</p><div class="row">${!s.user.checked?'<button class="btn coral" data-act="checkin">Check in</button>':'<span class="pill pGreen">Checked in</span>'}<button class="btn light" data-go="/ticket">Ticket</button></div></section><section class="reveal"><div class="revealHead"><span class="eyebrow" style="color:#ded9ff">Your group of three</span><h2>${esc(m.name)}</h2><p>Meet at ${esc(m.point)}</p></div><div class="revealBody"><div class="memberGrid">${ps.map(p=>`<div class="member"><span class="avatar" style="margin:0 0 9px;border:0">${esc(p.name.slice(0,2).toUpperCase())}</span><strong>${esc(p.name)}</strong><span>${esc(p.interest)} · ${esc(p.energy)}</span></div>`).join('')}</div><p class="note">No group chat. Meet at the event; contact exchange is optional after ratings.</p><div class="mission"><div class="between"><div><span class="eyebrow">Mission</span><h3>${esc(s.event.mission.name)}</h3></div>${pill(status(m))}</div><p class="muted">${esc(s.event.mission.brief)}</p><div class="stepsList">${s.event.mission.steps.map((x,i)=>`<div class="ms"><i>${i+1}</i><span>${esc(x)}</span></div>`).join('')}</div><div class="deadline"><div><b>${date(s.event.mission.deadline)}</b><div class="muted">Host-set evidence deadline</div></div>${missionAction(m)}</div>${e?`<div class="evidence"><b>${esc(e.title)}</b><p class="muted">${esc(e.note)}</p>${e.url?`<a href="${esc(e.url)}" target="_blank" style="color:var(--violet)">Open evidence link</a>`:''}</div>`:''}</div>${s.event.ended?ratings(m,ps):''}</div></section></div><aside class="panel"><span class="eyebrow">Two separate dates</span><h3>Event</h3><p class="muted">${date(s.event.date)}</p><h3>Mission deadline</h3><p class="muted">${date(s.event.mission.deadline)}</p><hr style="border:0;border-top:1px solid var(--line)"><p class="note">The host reviews submitted evidence later and marks it verified or requests an update.</p></aside></div></main>`}
function ratings(m,ps){return `<div style="margin-top:18px"><span class="eyebrow">After the event</span><h3>Rate your mix partners</h3>${ps.filter(p=>p.id!=='you').map(p=>{let r=s.ratings.find(x=>x.to===p.id)||{score:0,tags:[]};return `<div class="rating"><b>${esc(p.name)}</b><div class="stars">${[1,2,3,4,5].map(n=>`<button class="${n<=r.score?'on':''}" data-star="${n}" data-person="${p.id}">★</button>`).join('')}</div>${['Showed up','Respectful','Curious','Contributed'].map(t=>`<button class="tag ${r.tags.includes(t)?'on':''}" data-tag="${t}" data-person="${p.id}">${t}</button>`).join('')}</div>`}).join('')}</div>`}